[package]
name = "cargo-perf-benchmarks"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
regex = "1"
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "antipatterns"
harness = false
